# CodexGraph Debugger 完整流程说明

## 📋 总体流程概览

```
用户输入 (user_query + file_path)
    ↓
┌─────────────────────────────────────────┐
│  阶段1: 生成调试计划 (generate_debug_plan) │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  阶段2: 执行计划 (execute_plan_step)     │
│  - 按依赖关系执行步骤                    │
│  - 调用工具查询代码图谱                  │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  阶段3: 验证与调整 (validate_and_adjust) │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  阶段4: Bug定位 (locate_bug)             │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  阶段5: 生成修复方案 (generate_fix_solution)│
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  格式化输出 (_format_final_result)       │
└─────────────────────────────────────────┘
    ↓
最终报告 (Markdown格式)
```

---

## 🔍 详细流程说明

### 阶段1: 生成调试计划 (`generate_debug_plan`)

**目标**: 分析问题并制定结构化的多步骤调试计划

**输入**:
- `user_query`: 用户的问题描述
- `file_path`: 可选的文件路径

**处理过程**:
1. 构建问题上下文（包含在 `<issue>` 标签中）
2. 调用 LLM 生成 JSON 格式的调试计划
3. 解析 LLM 返回的 JSON，提取：
   - `analysis`: 问题分析
   - `steps`: 步骤列表，每个步骤包含：
     - `step_id`: 步骤ID
     - `goal`: 步骤目标
     - `tools`: 要使用的工具列表
     - `tool_args`: 工具参数列表
     - `expected_result`: 预期结果
     - `depends_on`: 依赖的步骤ID列表

**输出**: 调试计划字典

**失败处理**: 如果计划生成失败或为空，回退到 `_run_simple_flow`

---

### 阶段2: 执行计划 (`execute_plan_step`)

**目标**: 按计划执行工具调用，收集代码信息

**执行策略**:
- **依赖管理**: 按 `depends_on` 关系排序执行
- **并行执行**: 依赖已满足的步骤可以并行执行
- **循环保护**: 最多执行 `len(steps) * 2` 次迭代，防止无限循环

**每个步骤的执行流程**:
```
步骤开始
    ↓
遍历步骤中的每个工具
    ↓
调用 dispatch_function_call → 生成 Cypher 查询
    ↓
执行 Cypher 查询 (cypher_agent.run)
    ↓
收集查询结果
    ↓
记录执行状态（成功/失败）
```

**可用工具** (6个):
1. `find_nodes_in_file` - 查找文件中的所有节点
2. `find_class_by_keyword` - 按关键词查找类
3. `find_function_by_keyword` - 按关键词查找函数
4. `introduce_entity` - 介绍代码实体
5. `find_references` - 查找引用关系
6. `find_call_hierarchy` - 查找调用层级

**输出**: 执行结果列表，每个结果包含：
- `step_id`: 步骤ID
- `goal`: 步骤目标
- `results`: 工具执行结果列表
- `success`: 是否成功

---

### 阶段3: 验证与调整 (`validate_and_adjust_plan`)

**目标**: 检查执行结果，判断是否需要调整计划

**检查项**:
1. **失败步骤检查**: 统计失败的步骤数量
2. **结果充足性检查**: 检查是否收集到足够的信息
   - 如果结果为空或太少，标记需要补充步骤

**当前实现**: 仅记录和标记，不自动添加新步骤

**输出**: 调整后的计划（目前主要是原计划）

---

### 阶段4: Bug定位 (`locate_bug`)

**目标**: 基于收集的信息综合分析，定位疑似bug位置

**处理过程**:
1. **汇总信息**:
   - 问题分析
   - 执行步骤与结果
   - 查询到的代码节点信息（包括代码预览）

2. **构建定位Prompt**:
   - 包含所有收集的调试信息
   - 要求按照 `BUG_LOCALIZATION_FORMAT` 格式输出
   - 强调要明确指出具体的函数名、类名、文件路径

3. **LLM分析**: 调用 LLM 进行bug定位分析

**输出格式**:
```
[start_of_bug_locations]
### Text Description 1 for Bug Location
- Concise text description for the bug location: <描述>
- Why: <原因>

### Text Description 2 for Bug Location
...
[end_of_bug_locations]
```

---

### 阶段5: 生成修复方案 (`generate_fix_solution`)

**目标**: 基于收集的信息和bug定位结果，生成修复方案

**关键改进** (避免LLM编造代码):
1. **完整收集代码节点**:
   - 遍历所有执行结果
   - 收集所有包含代码的节点
   - 使用 `文件路径::节点名` 作为唯一标识去重

2. **智能优先级排序**:
   - 从bug定位结果中提取函数名/类名关键词
   - 按相关性对代码节点排序：
     - 节点名包含关键词: +10分
     - 文件路径包含关键词: +5分
     - 匹配用户提供的文件路径: +3分
   - 最相关的代码排在前面

3. **强化Prompt要求**:
   - 明确要求使用实际查询到的代码
   - 禁止编造代码
   - 如果代码较长，可以只显示关键部分，但必须从实际代码中提取

**输出**: 修复方案（包含 `<original>` 和 `<patched>` 标记）

---

### 最终格式化 (`_format_final_result`)

**目标**: 将所有结果整合成结构化的Markdown报告

**报告结构**:
1. **问题分析**: 从计划中提取
2. **调试计划**: 显示所有步骤的目标和工具
3. **执行结果摘要**: 显示每个步骤的执行状态和结果数量
4. **Bug定位**: bug定位结果
5. **修复方案**: 格式化的修复代码（使用 `markdown_answer` 处理）

---

## 🔄 回退流程 (`_run_simple_flow`)

当计划生成失败时使用，包含两种模式：

### 模式1: 简单工具调用
- 让LLM选择一个工具
- 执行查询并返回结果

### 模式2: 迭代流程
- 多轮迭代（`max_iterations`）
- 每轮分析上下文，决定是否继续搜索或定位bug
- 最后生成修复方案

---

## 🛠️ 工具函数详解

### 1. `find_nodes_in_file(keyword)`
- **用途**: 查找某个文件下的所有节点
- **Cypher**: `MATCH (n) WHERE n.file_path CONTAINS '{keyword}' RETURN ...`

### 2. `find_class_by_keyword(keyword)`
- **用途**: 查找类名中包含关键词的类
- **Cypher**: `MATCH (c:CLASS) WHERE c.name CONTAINS '{keyword}' RETURN ...`
- **语言兼容**: 自动适配 Python (CLASS) 和 C (STRUCT)

### 3. `find_function_by_keyword(keyword)`
- **用途**: 查找函数名中包含关键词的函数
- **Cypher**: `MATCH (f:FUNCTION) WHERE f.name CONTAINS '{keyword}' RETURN ...`

### 4. `introduce_entity(keyword)`
- **用途**: 介绍、讲解代码实体
- **Cypher**: `MATCH (n) WHERE n.name CONTAINS '{keyword}' RETURN ...`

### 5. `find_references(keyword)`
- **用途**: 查找某个实体的引用关系
- **Cypher**: `MATCH (a)-[r]->(t) WHERE t.name CONTAINS '{keyword}' RETURN ...`

### 6. `find_call_hierarchy(keyword)`
- **用途**: 查找函数/类的调用层级
- **Cypher**: 使用 `OPTIONAL MATCH` 查找上游调用者和下游被调用者
- **深度**: 默认 1..2 层

---

## 📊 数据流

```
用户查询
    ↓
调试计划 (JSON格式)
    ↓
步骤执行 → Cypher查询 → 图数据库 → 代码节点
    ↓
执行结果收集
    ↓
Bug定位分析
    ↓
代码节点收集与排序
    ↓
修复方案生成 (使用实际代码)
    ↓
格式化输出
```

---

## 🔑 关键特性

### 1. 依赖管理
- 支持步骤间的依赖关系
- 自动按依赖顺序执行
- 防止循环依赖

### 2. 代码真实性保证
- 完整收集所有查询到的代码节点
- 按相关性排序，优先使用相关代码
- 明确要求LLM使用实际代码，禁止编造

### 3. 错误处理
- 计划生成失败 → 回退到简单流程
- 工具执行失败 → 记录错误，继续执行
- 各阶段都有异常捕获和日志记录

### 4. 可观测性
- 每个阶段都有消息更新 (`update_agent_message`)
- 详细的调试日志
- 执行结果可视化

---

## 📝 输出示例

最终报告格式：
```markdown
## Debug 调试报告

### 1. 问题分析
[问题分析内容]

### 2. 调试计划
**步骤 1**: [目标]
- 工具: [工具列表]
- 预期结果: [预期结果]

### 3. 执行结果摘要
步骤 1: ✓ 成功
  - find_function_by_keyword: 找到 3 个结果

### 4. Bug 定位
[bug定位结果]

### 5. 修复方案
## Original: 
```python
[实际代码]
```

## Patched: 
```python
[修复后的代码]
```
```

---

## 🎯 优化点

1. **代码收集**: 确保所有查询到的代码都被使用
2. **智能排序**: 根据bug定位结果优先展示相关代码
3. **去重机制**: 避免重复添加相同节点
4. **错误恢复**: 多层次的错误处理和回退机制

