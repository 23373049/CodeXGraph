${message}

### Task Decision:
**CHECK YOUR CONVERSATION HISTORY.**
1. **IF** you have retrieved a list of files/nodes from a directory (e.g., via a `CONTAINS` query on a path), you **MUST** generate **BOTH**:
   - First, a **Module Analysis Report** (Option B).
   - Second, **Code Annotations** for the specific input code (Option A).
2. **ELSE** (if you only retrieved context for a single function/file), generate **Code Annotations** (Option A) only.

### Input Code/Context:
The user provided the following input (which may be a code snippet or a request):
```text
${file_path}
${user_query}
```

#### Final Output Format:

**Option A (Code Annotation - Default for single file):**
Return the fully annotated C code in a fenced block:
```c
{code_with_annotations}
```

**Option B (Module Analysis Report - REQUIRED for directory analysis):**
Return a Markdown report summarizing the module found in the context:
```markdown
# Module Analysis: [Directory Path]

## 1. Overview
[High-level summary of what this module/directory does based on the retrieved files]

## 2. File Structure & Key Components
- **[File Name]**: [Description of role/content]
  - *Key Functions/Macros*: [List important ones]
- **[File Name]**: ...

## 3. Architecture & Relationships
[Explain how these files interact (e.g., headers defining structs used in .c files)]
```

#### Comment Requirements:
1. Begin each function comment with `/**` and end with `*/`.
2. Include `@brief`, followed by a concise summary.
3. Add one `@param` line per parameter in declaration order, describing purpose and pointer direction (input/output).
4. Provide `@return` (or `@retval` entries) describing the meaning of the return value; for `void` functions, state `@return void`.
5. Optionally add `@note`, `@warning`, or `@retval` sections if there are notable edge cases or multiple return codes.

Example:
```c
/**
 * @brief Initialize the watchdog timer.
 *
 * @param timeout_ms Timeout in milliseconds (must be > 0).
 *
 * @return int 0 on success, negative errno code on failure.
 */
```